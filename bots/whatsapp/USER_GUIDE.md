# WhatsApp Bot User Guide

Welcome to your new WhatsApp Bot! This guide lists all available commands and how to use them.

## 🚀 Getting Started
1. Ensure the bot is running (`pnpm start`).
2. Scan the QR code if prompted.
3. Send commands to the bot or in a group where the bot is a participant.

## 🛠️ General Commands
| Command | Description | Example |
| :--- | :--- | :--- |
| `!help` | Shows a list of all available commands. | `!help` |
| `!ping` | Checks if the bot is responsive. | `!ping` |
| `!info` | Displays connection info. | `!info` |
| `!owner` | Shows the bot owner's contact info. | `!owner` |
| `!echo <text>` | Repeats the text you send. | `!echo Hello World` |

## 👥 Group Commands
*Note: Some commands require the bot to be an Admin.*

| Command | Description | Example |
| :--- | :--- | :--- |
| `!creategroup <name> <numbers>` | Creates a new group. | `!creategroup "My Group" 1234567890` |
| `!groupinfo` | Shows details about the current group. | `!groupinfo` |
| `!tagall` | Mentions everyone in the group. | `!tagall` |
| `!promote @user` | Promotes a user to Admin. | `!promote @JohnDoe` |
| `!demote @user` | Demotes an Admin to member. | `!demote @JohnDoe` |
| `!kick @user` | Removes a user from the group. | `!kick @Spammer` |
| `!revoke` | Revokes the current invite link. | `!revoke` |
| `!restrict` | Restricts group info editing to Admins. | `!restrict` |
| `!announcement` | Closes group chat (Admins only). | `!announcement` |

## 🖼️ Media Commands
| Command | Description | Example |
| :--- | :--- | :--- |
| `!sticker` | Converts an image/video/gif to a sticker. | Send image with caption `!sticker` |
| `!toimg` | Converts a sticker back to an image. | Reply to a sticker with `!toimg` |
| `!mediainfo` | Shows technical info about media. | Reply to media with `!mediainfo` |

## 👤 Contact & Profile
| Command | Description | Example |
| :--- | :--- | :--- |
| `!contact @user` | Sends the contact card of a user. | `!contact @Friend` |
| `!pfp @user` | Gets the profile picture of a user. | `!pfp @Friend` |
| `!myprofile` | Shows your own profile info. | `!myprofile` |

## 💬 Chat Management
| Command | Description | Example |
| :--- | :--- | :--- |
| `!mute` | Mutes the current chat. | `!mute` |
| `!unmute` | Unmutes the current chat. | `!unmute` |
| `!clear` | Clears all messages in the chat. | `!clear` |
| `!block @user` | Blocks a user. | `!block @Spammer` |
| `!unblock @user` | Unblocks a user. | `!unblock @Friend` |

## 🎭 Interactions
| Command | Description | Example |
| :--- | :--- | :--- |
| `!react <emoji>` | Reacts to a quoted message. | Reply with `!react 😂` |
| `!poll <Q> <Opt1> <Opt2>...` | Creates a poll. | `!poll "Pizza?" "Yes" "No"` |
| `!location` | Sends a sample location. | `!location` |
| `!status <text>` | Updates the bot's status/about. | `!status Busy coding` |

---
*Generated by Antigravity*
